# Display options

Options list **resolutions only** (`width × height`). Do not offer refresh-rate or vsync toggles.

The game **matches its container**: vsync on, paced by the monitor that currently owns the window. Dragging from a 144 Hz display to a 60 Hz display follows that monitor. Persist `WxH` (legacy `WxH@hz` keys still load as size only).
